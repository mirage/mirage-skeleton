language: c
install: wget https://raw.githubusercontent.com/ocaml/ocaml-travisci-skeleton/master/.travis-opam.sh
script: bash -ex .travis-opam.sh
env:
  - OCAML_VERSION=4.01 POST_INSTALL_HOOK="make build MODE=unix"
  - OCAML_VERSION=4.01 POST_INSTALL_HOOK="make build MODE=xen"
